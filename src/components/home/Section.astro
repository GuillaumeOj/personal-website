---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className = '' } = Astro.props;
---

<section id={id} class:list={['px-6 py-16 sm:py-24', className]}>
  <div class="mx-auto max-w-6xl">
    <slot />
  </div>
</section>
